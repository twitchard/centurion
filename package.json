{
  "name": "centurion-chess",
  "version": "2.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "dev:cloud": "vite --host 0.0.0.0 --open false",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.cli.json && tsc --noEmit -p tsconfig.api.json",
    "test": "vitest run",
    "check": "biome check src/ api/",
    "fix": "biome check --write src/ api/",
    "board:cli": "bun run src/cli/render-board.ts",
    "command:server": "bun run src/cli/compile-server.ts",
    "command:eval": "bun run src/cli/compile-eval.ts",
    "screenshot": "bun run scripts/screenshot.ts",
    "ci:all": "bun run check && bun run typecheck && bun run test && bun run build"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.110.0",
    "chessground": "^9.2.1",
    "chessops": "^0.15.0",
    "firebase": "^12.9.0",
    "stockfish": "^18.0.7"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@playwright/test": "^1.58.2",
    "@types/node": "^26.1.0",
    "bun-types": "^1.3.10",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^4.0.18"
  }
}
